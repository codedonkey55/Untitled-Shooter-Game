/// @description Insert description here
// You can write your code in this editor
if(path_position == 1) || (customEnd){
	slideTransition(transMode.next);
	if(coinsGained != 0){
		cursorObject.playerWallet += coinsGained;
		coinsGained = 0;
	}
}